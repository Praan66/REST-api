services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    # container_name: pgapi
    ports:
      - "8000:8000"
    env_file:
      - .env

    # image: pgapi
    command: >
      uv run uvicorn app.api.app:app
      --host 0.0.0.0
      --port 8000
      --reload


  # db:
  #   image: postgres
  #   container_name: postgres_db
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     - POSTGRES_DB: mydatabase
  #     - POSTGRES_USER: postgres
  #     - POSTGRES_PASSWORD: praan